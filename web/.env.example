# Hosted Passage account UI. Required for production auth handoff.
NEXT_PUBLIC_PASSAGE_HOSTED_URL=https://auth.haiven.example

# Set to production in deployed builds; local development keeps the fake adapter.
NEXT_PUBLIC_APP_ENV=local
